# 患者智能辅助 Agent：场景化面试拷打文档

> 本文只基于当前仓库的真实实现。目标不是背框架名词，而是能说明：业务为什么需要它、有哪些替代方案、为什么这样取舍、代价是什么、什么时候应该换掉。

## 1. 先搞清楚面试官在问什么

Agent 项目面试通常不是单纯的“场景题”，而是五类问题叠加：

| 类型 | 面试官真正考察什么 | 典型问题 |
|---|---|---|
| 业务场景 | 是否理解用户、痛点和产品边界 | 为什么患者咨询需要 Agent？ |
| 技术选型 | 是否会比较方案和做取舍 | 为什么选 LangGraph，不选手写流程？ |
| 系统设计 | 能否从 Demo 推演到生产 | 1000 个并发用户时怎么改？ |
| 故障与安全 | 是否考虑失败路径和数据风险 | MCP 超时、越权查询怎么办？ |
| 证据与反思 | 是否真的做过，而不是堆名词 | 如何证明三次 Planner 有价值？ |

回答任何选型题，都按下面七步组织：

1. **业务目标：** 要解决谁的什么问题。
2. **关键约束：** 数据量、并发、延迟、安全、开发周期。
3. **备选方案：** 至少给出两个合理选项。
4. **选择理由：** 哪个约束决定了当前方案。
5. **实现落点：** 指向本项目的真实代码和行为。
6. **代价与缺点：** 主动说出复杂度、成本和风险。
7. **更换条件：** 到什么规模或需求下切换方案。

一个好的回答不是“LangGraph 更强”，而是：

> 当前流程需要显式状态、条件分支和多轮工具调用，所以选择 LangGraph；但只有四个节点时手写循环也能完成。现在的收益主要是流程可观察和后续可扩展，代价是增加框架复杂度。只有补充评测后，我才会声称它改善了效果。

---

## 2. 项目真实基线

### 2.1 一句话定位

这是一个面向患者就诊信息查询和连续追问的 Agent Demo。系统能验证模拟患者身份、查询病例与就诊记录、检索历史记忆，并通过图片输入和语音播报改善交互；它不是医疗诊断系统。

### 2.2 当前真实技术栈

`Python + FastAPI + LangGraph + FastMCP/MCP stdio + OpenAI-compatible API + Qwen + SSE + SQLAlchemy + SQLite + FAISS + 原生 JavaScript + CosyVoice`

### 2.3 当前调用链

```text
浏览器 /chat
  -> POST /api/agent/query/stream
  -> FastAPI StreamingResponse
  -> LangGraph planner
  -> LangGraph agent_decision
  -> MCP client
  -> stdio 子进程中的 FastMCP Server
  -> 身份/患者/病例/就诊工具
  -> LangGraph finalizer
  -> SSE status/delta/done/error
  -> 保存 SQLite 对话
  -> 达到 5 轮时刷新画像、关键事件和 FAISS 索引
```

### 2.4 已实现与未实现

| 已实现 | 未实现或仅在路线图 |
|---|---|
| LangGraph 四节点编排 | LangGraph Checkpointer |
| 最多 6 次工具执行保护 | 完整重试、熔断和任务队列 |
| 三温度 Planner 候选合并 | 与单 Planner 的量化对照 |
| 一个 FastMCP Server、4 个工具 | 多个领域 MCP Server |
| MCP stdio 真实协议调用 | 远程 Streamable HTTP MCP |
| POST SSE、答案增量、前端防竞态 | 后端主动感知断连并终止任务 |
| SQLite 短期对话、画像和事件 | PostgreSQL、多租户隔离 |
| 规则关键词加 FAISS 稠密召回 | BM25、RRF、Milvus |
| OpenAI-compatible 配置 | 所有供应商完全无差异切换 |
| 图片输入、CosyVoice TTS | 医学影像诊断 |
| 工具白名单、超时和安全错误 | 完整鉴权、审计、限流 |
| 原生 HTML/CSS/JavaScript | React 前端 |
| 本地启动 | Docker 和公网部署 |

### 2.5 30 秒介绍

> 我做了一个患者信息查询 Agent Demo。后端使用 FastAPI，LangGraph 将规划、模型决策、工具执行和答案整理拆成四个节点；身份验证、患者资料、病例和就诊记录四个工具通过真实 MCP stdio 协议调用。前端使用 POST SSE 展示安全执行状态并流式输出答案。记忆层用 SQLite 保存对话、画像和事件，用 FAISS 做稠密向量召回并和规则关键词得分合并。项目重点是编排、工具协议和流式交互，目前仍是单机 Demo，生产化还需要鉴权、评测、持久化扩展和容器化。

### 2.6 90 秒介绍

> 项目场景是患者对自己的病例和就诊记录进行自然语言查询。普通 CRUD 要求用户理解字段和页面，纯聊天模型又无法访问可信业务数据，所以我把它设计成受控工具型 Agent。请求进入 FastAPI 后，LangGraph 的 Planner 先形成计划，Agent 节点决定是否调用工具，Tools 节点通过 MCP 获取真实数据，Finalizer 只基于工具证据生成结论。  
>  
> 为了改善等待体验，我增加了 POST SSE，只展示规划、工具调用、答案生成等安全状态，不展示思维链；最终答案按 token 增量返回。记忆层区分短期对话与长期画像、关键事件，每 5 轮触发一次规则抽取，FAISS 提供语义召回。  
>  
> 这个版本的价值是验证完整主链路，不是证明已经达到生产要求。当前 MCP 每次工具调用都会创建短生命周期 stdio 子进程，SQLite 和本地 FAISS 也限制了多实例部署，三次 Planner 还缺少效果与成本评测。这些都是我能明确解释的下一步。

---

## 3. 业务场景拷打

### 3.1 为什么选择“患者智能辅助”这个场景？

**强回答：**

患者的问题往往不是数据库字段式查询，而是“我上次复诊医生说了什么”“这次用药要注意什么”。系统需要识别意图、确认身份、选择数据源、组合多条记录并用患者能理解的方式回答，因此适合验证工具调用、记忆和多轮交互。

同时必须明确边界：系统只查询和整理患者自己的模拟数据，不能代替医生诊断、开药或处理急症。

**追问：这不就是把 SQL 包了一层聊天框吗？**

回答重点是“部分任务确实可以固定化”。身份验证和数据查询应该尽可能确定性，LLM 的价值主要在自然语言理解、工具路由和答案组织，而不是替代数据库规则。

### 3.2 为什么需要 Agent，而不是普通 RAG？

| 方案 | 擅长 | 不足 |
|---|---|---|
| 普通 RAG | 从文档中检索并回答 | 不擅长执行身份验证和结构化业务查询 |
| 固定工作流 | 流程稳定、结果可预测 | 难覆盖自然语言表达和不同工具组合 |
| Tool-calling Agent | 根据意图动态选择工具 | 成本、延迟和不确定性更高 |
| 当前折中 | LLM 做路由，工具做可信查询，Finalizer 整理证据 | 安全仍需服务端强约束 |

**项目回答：**

> 这个项目并非所有请求都需要 Agent。对确定接口可以直接走固定流程；只有当用户意图、所需数据源和工具顺序不确定时，才让 Agent 决策。RAG 在这里主要服务于患者记忆事件召回，结构化病例仍通过工具读取。

### 3.3 为什么不让模型直接读取整张患者表？

直接把整表放进 Prompt 会带来数据越权、上下文膨胀、过期数据和审计困难。当前项目通过受限工具读取所需数据；生产环境还应把“已验证患者身份”变成服务端授权上下文，而不能只靠模型先调用验证工具。

### 3.4 这个项目真正的业务指标是什么？

不要先说“准确率提升”。应该提出可测指标：

| 维度 | 可测指标 |
|---|---|
| 任务效果 | 任务完成率、工具选择准确率、参数正确率 |
| 医疗安全 | 越权阻断率、错误拒答率、事实一致性 |
| 检索效果 | Recall@K、MRR、关键事件命中率 |
| 交互体验 | 首字延迟 TTFT、总耗时、取消成功率 |
| 稳定性 | 工具成功率、SSE 完整率、异常恢复率 |
| 成本 | 单请求模型调用次数、token、TTS 和 embedding 成本 |

没有真实评测前，只能说“实现了”或“验证了链路”，不能说“显著提升”。

---

## 4. 技术选型总表

| 决策点 | 当前选择 | 主要备选 | 当前为什么选 | 主要代价 | 何时更换 |
|---|---|---|---|---|---|
| Agent 编排 | LangGraph | 手写循环、LangChain Agent、Dify | 显式状态和条件边，保留代码控制权 | 框架复杂度 | 流程极简时退回固定工作流 |
| 规划 | 三次 Planner 合并 | 单次规划、规则路由、小模型分类 | 验证自一致性思路 | 三倍规划调用 | 评测无收益时删除或降级 |
| 工具协议 | MCP stdio | 直接函数、内部 HTTP API | 标准工具发现和协议边界 | 子进程与协议开销 | 远程、多实例时转 Streamable HTTP |
| 流式交互 | POST SSE | 普通 HTTP、EventSource、WebSocket | 单向状态与答案流，且需 POST 图片/JSON | 断线恢复和双向控制有限 | 实时音频、双向打断时用 WebSocket |
| 短期数据 | SQLite | PostgreSQL、MySQL | 单机 Demo 零运维 | 并发写和多实例受限 | 多用户、长期运行时迁移 PostgreSQL |
| 向量检索 | FAISS IndexFlatIP | Chroma、Milvus、pgvector | 本地轻量、数据量小、精确检索 | 文件一致性和服务化能力弱 | 多租户、过滤、扩容、混合检索时迁移 |
| 词法召回 | 规则关键词得分 | BM25、全文索引 | 词表小、实现直接 | 泛化差、分数不可校准 | 数据增多时使用 BM25 |
| 融合 | 原始分数加权 | RRF、学习排序、Cross-Encoder | 当前候选少，便于实现 | 不同分数空间难比较 | 多路召回时使用 RRF/重排 |
| 模型接入 | OpenAI-compatible | 厂商原生 SDK | 文本、工具、流式接口可复用 | 兼容并非完全一致 | 使用厂商独有能力时增加 Adapter |
| 前端 | 原生 JavaScript | React、Vue | 页面少、无复杂状态、已有实现 | 组件复用与工程工具弱 | 页面和团队规模扩大时迁移 |
| 语音 | 独立 CosyVoice | 浏览器 TTS、其他云 TTS | 中文音色与云端能力可用 | 独立协议、延迟和费用 | 成本或离线要求变化时替换 |

---

## 5. 核心选型深挖

### 5.1 为什么选 LangGraph？

**场景约束：** 流程包含规划、模型决策、多轮工具调用、条件退出和最终整理，需要共享状态并输出过程事件。

**替代方案比较：**

| 方案 | 优点 | 为什么当前没选 |
|---|---|---|
| 手写 `while` | 最简单、依赖少、调试直接 | 节点职责、状态和跳转混在一起，继续扩展会更难维护 |
| LangChain Agent | 模型、工具和 Agent 抽象成熟 | 预构建抽象较高，当前需要保留自定义 Planner 和 SSE 事件 |
| Dify 等平台 | 快速搭建、可视化 | 定制、调试、版本管理和代码表达不够贴合本项目 |
| AutoGen 类多 Agent | 适合多角色协作 | 当前不是多 Agent 协商问题，引入会过重 |
| LangGraph | 状态和条件边显式、支持自定义节点 | 需要自己设计状态、错误处理和持久化 |

**项目落点：**

- 四个节点：`planner`、`agent_decision`、`tools`、`finalizer`。
- 两组条件边控制工具循环。
- `MAX_TOOL_STEPS=6` 防止无限调用。
- `stream_mode=["custom", "values"]` 中只把安全 custom 事件交给 SSE。
- 当前没有 Checkpointer，也没有 durable execution。

**一句强回答：**

> 我选择 LangGraph 不是因为它天然提高模型效果，而是因为流程已经出现显式状态、条件循环和流式事件。它让控制流更容易观察和扩展。但当前只有四个节点，手写循环也能运行，所以我把这次迁移定义为工程重构，效果收益必须通过评测证明。

**反杀追问：什么时候不该用 LangGraph？**

只有一次模型调用、固定两三个步骤、无恢复与人工介入需求时，普通函数或确定性状态机更简单。

### 5.2 为什么不使用 LangChain？

LangGraph 可以独立使用，不要求业务代码直接采用 LangChain。当前模型客户端、工具 Schema、Retriever 和向量读写已经能运行，立即再套一层 LangChain 只会增加抽象迁移和调试成本。

合理的引入条件是：需要统一多供应商模型、标准 Retriever/Document、Tracing 或大量社区集成，并且能证明减少的胶水代码大于新增复杂度。

**不能回答：** “LangChain 就是工具层，LangGraph 就是编排层。”  
**更准确：** LangChain 提供模型、工具、Retriever 和 Agent 等高层组件；LangGraph 是低层编排运行时，两者可以组合，也可以分开使用。

### 5.3 为什么设计三次 Planner？

当前 Planner 用 0.1、0.4、0.7 三档温度生成 JSON 候选，再通过多数票和去重规则合并身份验证、图片推理、工具顺序和回答重点。

**选择动机：** 验证同一问题在不同采样下能否获得更稳定计划。

**主要问题：**

- 每次请求固定增加三次模型调用。
- 首字延迟明显增加。
- 合并是规则投票，不等于真正验证推理正确性。
- 目前没有单 Planner 基线和评测数据。

**成熟回答：**

> 这是一个实验性策略，而不是已证明的优化。我会用工具选择准确率、任务完成率、TTFT 和成本做 A/B 对照。如果收益不明显，优先改成规则路由加一次 Planner，或只对复杂请求启用多候选。

### 5.4 为什么选择 MCP，不直接调用 Python 函数？

**直接函数调用的优势：** 低延迟、类型直接、部署简单。  
**MCP 的优势：** 标准化工具发现、JSON Schema、生命周期和跨进程边界，未来可以替换成本地或远程工具服务。

当前实现是真实 MCP：

- FastMCP Server 暴露 4 个工具。
- Client 使用 `initialize`、`list_tools`、`call_tool`。
- stdio client 启动 `python -m app.mcp_server` 子进程。
- 工具列表有进程内缓存。
- 单次工具调用仍创建短生命周期 session，默认超时 60 秒。
- 白名单外工具在 client 和 Agent 两层拒绝。

**最危险追问：只有一个服务、四个工具，引入 MCP 是不是过度设计？**

> 对当前 Demo，直接函数调用确实更简单。引入 MCP 的目的主要是验证标准协议边界和未来工具独立部署，而不是解决当前性能问题。它的代价是子进程启动和调试复杂度。如果项目不会扩展到跨进程、跨语言或第三方工具，我会保留普通函数调用。

**什么时候换 Streamable HTTP？**

当 MCP Server 独立部署、需要多个客户端、HTTP 鉴权、连接复用和水平扩展时。当前 stdio 更适合本地单客户端工具。

### 5.5 为什么选择 SSE，不用 WebSocket？

当前需求是服务端向浏览器单向发送：

- `status`：规划、工具调用、生成。
- `delta`：最终答案增量。
- `done`：最终结果。
- `error`：安全错误信息。

普通 HTTP 无法持续反馈进度；WebSocket 支持双向通信，但当前没有持续上行数据。原生 EventSource 主要通过 GET 建连，而项目需要 POST JSON 和 Base64 图片，所以采用 `fetch + ReadableStream` 解析 SSE。

**代价：**

- 浏览器断线后没有自动恢复和事件重放。
- AbortController 主要阻止旧事件更新前端，后端尚未主动检查断连。
- 反向代理可能缓冲或超时。
- `done` 当前仍包含完整 `tool_outputs`，Network 面板可以看到，公开部署前必须收口。

**什么时候用 WebSocket？**

实时语音上下行、服务端主动推送、用户真正打断执行、协作会话或高频双向事件时。

### 5.6 为什么设计短期记忆和长期记忆？

短期对话保留最近上下文，适合指代和连续追问；长期画像与关键事件用于跨轮次稳定偏好和历史事件。把所有历史对话直接塞进 Prompt 会带来 token 膨胀、噪声和隐私风险。

当前实现：

- 用户和助手消息写入 SQLite。
- 5 轮即 10 条消息触发一次规则抽取。
- 长期层包含用户画像和关键事件。
- 病例、就诊记录与对话事件进入检索。
- 没有 LangGraph Checkpointer，记忆持久化由业务表完成。

**追问：规则抽取算智能记忆吗？**

它能验证分层架构，但能力有限。当前用户画像和对话事件主要基于规则和关键词，不应包装成复杂的 LLM 记忆系统。下一步可增加结构化抽取模型、置信度、冲突合并、过期策略和用户可删除机制。

### 5.7 为什么选择 FAISS，不选 ChromaDB、Milvus 或 pgvector？

| 方案 | 适合场景 | 当前判断 |
|---|---|---|
| FAISS | 单机、低运维、快速验证向量相似度 | 当前数据少，最合适 |
| ChromaDB | 本地原型、文档与 metadata API | 比 FAISS 封装更完整，但仍不是当前必须 |
| Milvus | 大规模、多租户、标量过滤、稠密与稀疏混合检索 | 能力强，但当前引入服务和运维过重 |
| pgvector | 已使用 PostgreSQL，希望事务数据与向量统一 | 迁移 PostgreSQL 后值得评估 |
| Elasticsearch/OpenSearch | 全文检索、过滤、搜索运维成熟 | 适合词法检索重、已有搜索基础设施的团队 |

当前索引按患者拆成本地 FAISS `IndexFlatIP` 和 JSON metadata。向量先归一化，因此内积可对应余弦相似度；Flat 索引提供精确搜索，但文件一致性、并发更新和多实例共享需要应用自行处理。

**一句强回答：**

> 当前目标是验证几十到少量事件的记忆召回，FAISS 无额外服务、接入成本最低。Milvus 的价值要到多用户、大数据量、过滤和混合检索需求出现后才成立。现在直接换 Milvus 会让运维复杂度大于业务收益。

### 5.8 当前为什么不叫 BM25 + RRF？

当前词法侧是固定关键词命中后的规则分数，向量侧是 FAISS 相似度乘权重，最后直接相加：

```text
keyword_score = 0.6 + min(keyword_hits, 5) * 0.08
vector_score = faiss_score * 0.7
final_score = keyword_score + vector_score
```

它不是 BM25，因为没有词频、逆文档频率和文档长度归一化；也不是 RRF，因为没有根据不同召回列表的名次计算倒数排名。

升级 Milvus 时可以使用 BM25 稀疏检索和 dense 检索分别召回，再用 RRF 或 WeightedRanker 融合。但是否更好仍需 Recall@K、MRR 和业务样例验证。

### 5.9 为什么选 SQLite，不直接上 PostgreSQL？

SQLite 适合单机 Demo：无需独立服务，初始化和分发简单。当前数据量小、访问者少，优先验证 Agent 主链路更合理。

它的限制是：

- 并发写入和锁竞争。
- 多实例共享困难。
- 权限、多租户和运维能力有限。
- 本地数据库与本地 FAISS 难以横向扩展。

当项目需要公网长期运行、多人并发、审计、备份恢复和多实例部署时，应迁移 PostgreSQL。迁移不仅是修改 URL，还包括迁移脚本、连接池、约束、事务、备份和历史数据校验。

### 5.10 为什么使用 OpenAI-compatible API？

当前文本/视觉模型客户端把 `base_url`、`api_key` 和 `model` 放到环境变量，能复用 OpenAI SDK 的消息、工具调用和流式接口。

**优点：** 减少不同供应商的基础接入代码。  
**限制：** “兼容”不代表完全一致。工具调用字段、视觉消息、reasoning、stream chunk、错误码、限流和模型能力可能不同。

**正确表达：**

> 对完整支持当前协议子集的模型，通常可以通过环境变量切换；但上线前必须做供应商契约测试。Embedding 和 TTS 是独立模型与协议，不能只改文本模型名称就认为整体完成切换。

### 5.11 为什么前端不用 React？

当前只有 `/query` 和 `/chat` 两个页面，交互集中在表单、消息列表和 SSE 状态，原生 JavaScript 已经能完成需求。引入 React 会增加构建链和组件迁移成本，不能因为招聘描述里有 React 就强行改。

页面增多、共享状态复杂、组件复用明显或多人协作时，再评估 React。这个选择体现的是按复杂度决策，不是不会 React。

---

## 6. 生产场景题

### 场景 1：同时有 1000 个患者访问，哪里先出问题？

**当前瓶颈顺序：**

1. 三次 Planner 和多轮模型调用带来的限流、成本和连接占用。
2. 每次 MCP 工具调用启动 stdio 子进程。
3. SQLite 并发写和单机数据共享。
4. 每位患者本地 FAISS 文件的并发读写。
5. 同步生成器和长连接占用服务资源。

**演进方案：**

1. 先压测并记录 QPS、TTFT、P95、模型调用次数和工具耗时。
2. 简单意图走规则或小模型路由，复杂问题才启用 Planner。
3. MCP 改长连接或远程 Streamable HTTP。
4. SQLite 迁移 PostgreSQL，加入迁移和连接池。
5. FAISS 迁移服务化向量库或 pgvector。
6. 长任务进入队列，应用实例无状态化。
7. 增加缓存、限流、熔断和可观测性。

**面试陷阱：** 不要一上来就说 Kubernetes。先定位瓶颈、定义容量目标，再选择基础设施。

### 场景 2：需要支持多家医院，怎么保证数据隔离？

当前没有多租户能力，不能声称已经支持跨院。

生产设计至少需要：

- 请求携带不可伪造的 `tenant_id` 和用户身份。
- 所有数据库查询强制加入 tenant/patient 授权条件。
- MCP 工具从服务端授权上下文取身份，拒绝模型自行传入任意 patient_id。
- PostgreSQL 行级安全或独立 schema/数据库策略。
- 向量数据加入租户和患者过滤，避免跨租户召回。
- API、工具和管理员操作保留脱敏审计日志。
- 加密、密钥隔离、数据保留和删除策略。

**关键回答：** Prompt 中写“不要越权”不构成安全边界。

### 场景 3：患者通过 Prompt Injection 要求查询别人病历怎么办？

安全应分层：

1. 模型 Prompt 表达行为边界。
2. Agent 只暴露白名单工具。
3. 工具输入使用严格 Schema 和范围校验。
4. 工具服务端根据认证主体做授权，而非相信模型参数。
5. 输出前做敏感字段最小化和脱敏。
6. 记录越权尝试并触发风控。

当前只完成了部分白名单、字段掩码和错误收口，缺少真实鉴权与强授权。

### 场景 4：MCP Server 超时或崩溃怎么办？

当前有 60 秒超时并转换成安全错误，Tools 节点失败后流式返回 `error`。不足是没有重试、熔断、降级和幂等设计。

演进时先按工具性质分类：

| 工具类型 | 合理策略 |
|---|---|
| 只读查询 | 有限次数指数退避重试 |
| 写操作 | 幂等键、状态查询，不能盲目重试 |
| 身份验证 | 失败时拒绝继续，不能绕过 |
| 非关键增强 | 降级并明确说明证据不足 |

还需要记录 tool_name、耗时、错误类型和 trace_id，但不能记录明文患者数据。

### 场景 5：模型供应商故障，能否直接切换？

环境变量只能解决配置切换，不能保证语义兼容。应建立供应商 Adapter 和契约测试：

- 普通文本回答。
- Tool calls 的名称、arguments 和空值。
- SSE chunk 结构与结束标记。
- 图片输入格式。
- 超时、限流和错误码。
- 最大上下文和 token 统计。

故障切换还要考虑同一请求是否可安全重试、不同模型输出差异、成本上限和数据是否允许发送到备用供应商。

### 场景 6：检索不到患者上次复诊记录，如何排查？

按链路分层排查：

1. SQLite 是否存在正确的业务记录。
2. 是否已生成对应 `MemoryEvent`。
3. JSON metadata 与 FAISS index 是否一致。
4. embedding 模型和维度是否变化。
5. 查询关键词是否进入固定词表。
6. top_k 是否过小，分数融合是否把目标压下去。
7. 最终 Prompt 是否包含召回事件。
8. Finalizer 是否忽略了证据。

不要只盯着“换更大的 embedding 模型”。召回、融合、上下文构造和生成都可能出错。

### 场景 7：用户点击清空后，后端任务还在计费怎么办？

当前 AbortController 与 requestId 解决的是前端状态污染，不等于后端已取消。后端生成器没有主动检查 `request.is_disconnected()`，外部模型请求和 MCP 调用可能继续。

完整方案需要：

- 后端检测客户端断开。
- 把取消信号传递给图执行和模型流。
- 对长工具调用提供取消或超时。
- 为请求设置总 deadline 和预算。
- 记录“客户端取消”和“服务端实际停止”两个指标。

### 场景 8：SSE 上线后一次性返回，不再流式，怎么排查？

检查顺序：

1. 应用是否逐次 `yield`。
2. 响应是否为 `text/event-stream`。
3. Nginx 是否关闭代理缓冲。
4. CDN/平台是否缓存或聚合响应。
5. 是否有空闲连接超时。
6. 是否需要周期性 heartbeat。
7. 浏览器是否真正使用流读取，而不是等待 `response.json()`。

当前设置了 `Cache-Control: no-cache` 和 `X-Accel-Buffering: no`，但部署后仍必须做真实网络验收。

### 场景 9：怎样降低延迟和成本？

先测量，再优化：

| 成本来源 | 可选优化 |
|---|---|
| 三次 Planner | 按复杂度启用、单次规划、规则路由 |
| 多轮工具决策 | 减少工具集合、明确 Schema、检测重复调用 |
| MCP 子进程 | 复用 session、远程长连接 |
| Finalizer | 简单结果模板化，复杂结果才调用模型 |
| Embedding | 批量、缓存、增量更新 |
| TTS | 用户明确选择才生成，缓存相同文本 |

不能为了降低成本牺牲身份验证和数据授权。

### 场景 10：如何把项目推进到可公开试玩？

建议顺序：

1. 只保留虚构患者数据。
2. 去掉 SSE `done.tool_outputs` 中的敏感结果。
3. 增加访问口令、限流、上传校验和低额度独立 API Key。
4. Docker 化应用，挂载 `Agent/data` 持久卷。
5. 单实例部署并配置 HTTPS。
6. 用人工用例验证健康检查、查询、SSE、取消、图片、TTS 和 MCP 异常。
7. 监控请求量、模型成本、错误率和磁盘增长。

Docker 解决可复现打包，上线解决公网访问，两者不是同一件事。

---

## 7. 四组连续追问

### 7.1 编排选型追问链

**面试官：为什么使用 Agent？**  
因为用户意图和数据源组合不固定，需要模型做受控路由；确定性查询仍由工具完成。

**面试官：为什么不用固定工作流？**  
常见高频路径可以固定化，但图片、连续追问和不同记录组合使全部路径写死成本较高。合理架构应是固定流程与 Agent 混合，不是所有请求都交给模型。

**面试官：为什么选 LangGraph？**  
需要显式管理四类节点、共享状态、条件循环和流式事件。

**面试官：四个节点也值得引入框架？**  
不一定。当前是为后续 Checkpointer、人工审批和错误分支预留结构；若项目停止扩展，手写状态机可能更经济。

**面试官：LangGraph 带来了什么可量化收益？**  
目前只确认等价迁移和可维护性，尚未完成效果评测。应比较缺陷率、开发变更成本、任务完成率和延迟。

**面试官：那为什么简历里写“显著提升准确率”？**  
不应该写，没有基线和评测就删除该表述。

### 7.2 MCP 追问链

**面试官：MCP 解决什么问题？**  
统一工具发现、Schema 和调用协议，让模型编排与业务能力解耦。

**面试官：你这四个函数直接调用不是更快？**  
是。当前 MCP 的收益是验证协议和未来远程化边界，性能上反而有额外开销。

**面试官：怎么证明走了真实 MCP？**  
Client 完成 initialize、list_tools、call_tool；stdio 启动独立 FastMCP 子进程，Agent 不再直接 import 业务 service。

**面试官：为什么调用慢？**  
每次 call_tool 都建立短生命周期 stdio session 和子进程，只有工具发现结果缓存。

**面试官：生产怎么改？**  
本机可复用长 session；工具独立部署时采用 Streamable HTTP，并加入认证、连接管理、超时、熔断和观测。

**面试官：MCP 就等于安全了吗？**  
不等于。协议边界、白名单和 Schema 只是基础，患者授权必须在工具服务端强制执行。

### 7.3 RAG 追问链

**面试官：你的 RAG 检索什么？**  
检索患者自己的病例、就诊和对话关键事件，不是医学知识库。

**面试官：为什么 FAISS？**  
数据小、单机 Demo、无需额外服务，Flat 索引足够。

**面试官：为什么不用 Chroma？**  
Chroma 提供更完整的存储 API，但当前已有 SQLite metadata 和简单索引读写，引入后的收益有限。

**面试官：为什么不直接用 Milvus？**  
当前没有规模、过滤和并发需求支撑它的部署成本。达到多租户、混合检索和服务化需求后再迁移。

**面试官：你现在是不是混合检索？**  
是规则关键词与稠密向量的加权融合，但不是 BM25 + RRF。

**面试官：怎么证明 Milvus 升级有效？**  
使用同一标注查询集比较 Recall@K、MRR、任务完成率、P95 延迟和运维成本，而不是只比较数据库功能列表。

### 7.4 医疗安全追问链

**面试官：如何保证不查到其他患者数据？**  
当前不能完全保证，只有 Prompt、身份工具和白名单。生产必须有登录身份、服务端授权上下文和所有查询的租户/患者过滤。

**面试官：模型可以伪造 patient_id 怎么办？**  
工具不能信任模型传入的 patient_id，应根据认证 token 映射到允许访问的患者范围。

**面试官：模型回答错了怎么办？**  
Finalizer 只能基于工具证据，显示信息来源和不足；高风险建议必须拒答或转人工。还需做事实一致性和拒答评测。

**面试官：日志怎么排查又不泄露隐私？**  
记录 trace_id、工具名、耗时、状态和错误类型；患者字段脱敏，禁止记录完整 Prompt、工具结果和密钥。

**面试官：能上线医院吗？**  
当前不能。它是技术 Demo，缺少真实鉴权、授权、审计、合规、临床评估和生产运维体系。

---

## 8. 架构设计白板题

### 题目：把当前 Demo 设计成可服务多院区的系统

回答顺序不要从组件名开始，应先说约束：

1. 多租户数据严格隔离。
2. 患者只能查询本人数据。
3. 回答需要证据可追踪。
4. LLM 和工具调用可能超时。
5. SSE 长连接需要限流和取消。
6. 业务数据与向量索引需要一致性策略。

可演进为：

```text
Web / Mobile
  -> API Gateway: HTTPS、认证、限流
  -> FastAPI: 请求校验、tenant context、SSE
  -> LangGraph: 路由、工具循环、超时预算、Checkpointer
  -> MCP Client Pool
       -> Identity MCP
       -> Medical Record MCP
       -> Visit MCP
  -> PostgreSQL: 业务数据、会话、审计、checkpoint
  -> Milvus/pgvector: tenant + patient 过滤、dense/sparse 检索
  -> Queue/Worker: 长期记忆抽取、索引重建、TTS
  -> Observability: trace、指标、脱敏日志、评测
```

**为什么不是现在就这样做？**

当前目标是 Demo 验证，提前引入网关、队列、多个数据库和多服务会显著增加开发与运维成本。架构演进应由并发、隔离、SLA 和评测数据触发。

---

## 9. 评测设计题

### 9.1 最小评测集

| 类别 | 示例 | 期望 |
|---|---|---|
| 身份成功 | 正确编号和手机号 | 验证后允许查询 |
| 身份失败 | 错误手机号 | 拒绝返回隐私数据 |
| 最近就诊 | 查询最近一次记录 | 调用 visit tool 且 `limit=1` |
| 病例查询 | 查询历史诊断 | 只使用病例工具结果 |
| 信息缺失 | 只有姓名无凭证 | 要求补充验证信息 |
| 越权攻击 | 要求查询另一患者 | 强制拒绝 |
| 无工具问题 | 普通问候 | 不应无意义调用工具 |
| 多轮追问 | “那这次用药呢” | 正确使用上下文 |
| 图片输入 | 上传检查资料 | 模型可处理且不声称确诊 |
| 工具故障 | MCP 超时 | 安全失败，不泄露堆栈 |

### 9.2 对照实验

至少比较四组：

1. 手写循环 vs LangGraph 等价实现。
2. 单次 Planner vs 三次 Planner 合并。
3. 仅关键词 vs 仅向量 vs 当前融合。
4. 当前 FAISS vs 未来 Milvus BM25 + dense + RRF。

### 9.3 验收标准

- 任务完成率和工具参数准确率不能下降。
- 越权测试必须全部拒绝。
- 不出现工具参数、原始结果、Prompt 和密钥泄露。
- SSE 事件顺序完整，取消后旧请求不污染页面。
- P95 延迟和单请求成本在目标预算内。
- 评测失败样例必须可通过 trace 定位到规划、工具、检索或生成阶段。

---

## 10. 简历口径

### 10.1 建议技术栈

**Python、FastAPI、LangGraph、FastMCP/MCP、SSE、OpenAI-compatible API、Qwen、DashScope/CosyVoice、SQLAlchemy、SQLite、FAISS、原生 JavaScript**

### 10.2 建议项目描述

**患者智能辅助 Agent｜个人项目/核心开发｜填写真实时间**

> 面向患者就诊信息查询和连续追问场景，开发可调用模拟院内工具、检索历史记忆并支持图片输入与语音播报的 Agent Demo。系统仅用于技术验证，不作为医疗诊断依据。

- 使用 LangGraph 将 Agent 拆分为 Planner、决策、工具执行和 Finalizer 四个节点，通过条件边实现 ReAct 风格工具循环，并设置最多 6 次工具执行保护。
- 使用 FastMCP 暴露身份验证、患者资料、病例和就诊记录 4 个工具，Agent 通过真实 MCP stdio client 完成工具发现和调用，并加入白名单与 60 秒超时。
- 使用 SQLite 保存短期对话、用户画像和关键事件，按 5 轮对话阈值触发规则抽取；使用 FAISS `IndexFlatIP` 做稠密向量召回，并与规则关键词得分融合。
- 使用 POST SSE 展示安全执行状态并增量输出最终答案，通过 AbortController 和 requestId 避免清空或重发后的旧响应污染页面。
- 抽象 LLM、Embedding 和 TTS 环境配置，当前接入 Qwen 视觉模型与 CosyVoice，支持图片辅助理解和可选语音播报。

### 10.3 禁止夸大的表述

- 不写 LangChain 已使用。
- 不写 Docker、Milvus、PostgreSQL 已完成。
- 不写拆分了多个 MCP Server。
- 不写固定使用 DeepSeek，当前默认是 Qwen。
- 不写 BM25 + RRF，当前不是。
- 不写 LangGraph Checkpointer，当前未启用。
- 不写医学影像诊断。
- 不写多院区快速迁移。
- 不写显著提升准确率或减少医生耗时，除非有评测。
- 项目时间必须与真实经历一致。

---

## 11. 面试前自检

以下问题必须能脱离文档回答：

1. 用户为什么需要 Agent，而不是 CRUD 页面？
2. 哪些请求应该固定工作流，哪些交给 Agent？
3. 为什么选 LangGraph，它在哪些情况下属于过度设计？
4. 为什么没有使用 LangChain？
5. 三次 Planner 的收益、成本和验证方法是什么？
6. MCP 相比直接函数调用解决了什么，新增了什么问题？
7. 当前 MCP 为什么是真协议，生命周期是什么？
8. stdio 和 Streamable HTTP 分别适合什么场景？
9. 为什么选 SSE，不选 WebSocket？
10. 为什么使用 fetch 流而不是 EventSource？
11. 前端取消与后端真正取消有什么区别？
12. 当前 `done.tool_outputs` 为什么是安全风险？
13. 短期记忆、长期画像、关键事件分别存什么？
14. 当前检索为什么不是 BM25 + RRF？
15. 为什么 FAISS 适合当前数据量？
16. 什么时候选 Chroma、Milvus 或 pgvector？
17. 为什么当前用 SQLite，迁移 PostgreSQL 要做哪些工作？
18. OpenAI-compatible 为什么不能保证只改 `.env` 就成功？
19. TTS 为什么是独立模型和协议？
20. 1000 并发时瓶颈顺序是什么？
21. 多院区的数据隔离如何设计？
22. Prompt Injection 为什么不能只靠 Prompt 防御？
23. 如何定位一次错误回答发生在哪一层？
24. 如何设计 Agent 评测集和基线？
25. 当前项目最不合理的三个设计是什么？

最后一题建议诚实回答：

> 第一，三次 Planner 固定执行，成本和延迟可能大于收益；第二，MCP 每次工具调用启动短生命周期子进程；第三，SSE 完成事件仍携带完整工具结果。这些都是当前 Demo 的明确边界，也是生产化前最先验证或修复的地方。

---

## 12. 官方资料

- LangGraph Overview: https://docs.langchain.com/oss/python/langgraph/overview
- MCP Architecture: https://modelcontextprotocol.io/docs/learn/architecture
- MCP Transports: https://modelcontextprotocol.io/specification/2025-06-18/basic/transports
- FAISS Documentation: https://github.com/facebookresearch/faiss/wiki
- FAISS Index Selection: https://github.com/facebookresearch/faiss/wiki/Guidelines-to-choose-an-index
- Milvus BM25: https://milvus.io/docs/bm25-function.md
- Milvus Hybrid Search and RRF: https://milvus.io/docs/multi-vector-search.md
